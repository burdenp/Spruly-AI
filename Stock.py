import sys
import os
import string

class stock():
	def __init__(self, name, cost, time, views, volume, mcap, pchange, perchange):
		self.name = name
		self.price = price
		self.time = time
		self.views = views
		self.volume = volume
		self.mcap = mcap
		self.pchange = pchange
		self.perchange = perchange
		self.weight = -1

